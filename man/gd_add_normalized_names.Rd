% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-names.R
\name{gd_add_normalized_names}
\alias{gd_add_normalized_names}
\title{Agregar columnas con nombres geográficos normalizados}
\usage{
gd_add_normalized_names(
  data,
  .cols = NULL,
  .suffix = "_norm",
  .clean = FALSE,
  .levels = NULL,
  .tol = 0.25,
  .on_error = "na"
)
}
\arguments{
\item{data}{Un data frame.}

\item{.cols}{Vector de nombres de columnas a normalizar. Si es \code{NULL}, se
usan columnas geográficas detectadas por nombre, como \code{provincia},
\code{municipio}, \code{region}, \code{dm}, \code{seccion}, \code{barrio_paraje} o \code{zona}.}

\item{.suffix}{Sufijo para las columnas nuevas. Por defecto \code{"_norm"}.}

\item{.clean}{Lógico. Si \code{TRUE}, corrige primero los nombres usando los
cleaners oficiales de geodom. Por defecto \code{FALSE}.}

\item{.levels}{Vector opcional con los niveles administrativos de \code{.cols}.
Puede ser nombrado, por ejemplo \code{c(provincia = "provinces")}. Niveles
válidos: \code{"regions"}, \code{"provinces"}, \code{"municipalities"}, \code{"dm"},
\code{"sections"}, \code{"bparajes"} y \code{"zones"}.}

\item{.tol}{Tolerancia para los cleaners cuando \code{.clean = TRUE}.}

\item{.on_error}{Manejo de errores en los cleaners cuando \code{.clean = TRUE}:
\code{"fail"}, \code{"na"} u \code{"omit"}.}
}
\value{
\code{data} con columnas adicionales normalizadas.
}
\description{
Agrega columnas normalizadas a partir de columnas geográficas existentes.
Por defecto normaliza los valores existentes. Si \code{.clean = TRUE}, primero
corrige cada valor usando el cleaner oficial del nivel administrativo y luego
normaliza el resultado.
}
\examples{
datos <- data.frame(
  provincia = c("San Cristóbal", "Duarte"),
  municipio = c("Villa Altagracia", "San Francisco de Macorís")
)

gd_add_normalized_names(datos)
}
