% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactive.R
\name{gd_map_interactive}
\alias{gd_map_interactive}
\title{Crear un mapa interactivo HTML portable}
\usage{
gd_map_interactive(
  data,
  fill = NULL,
  .level = NULL,
  .name = NULL,
  .key = NULL,
  file = NULL,
  title = "Mapa GeoDOM",
  subtitle = NULL,
  caption = NULL,
  labels = FALSE,
  background = "none",
  context = TRUE
)
}
\arguments{
\item{data}{Un data frame con los datos a mapear.}

\item{fill}{Nombre de la variable para fill. Si es NULL, se detecta automáticamente.}

\item{.level}{Nivel administrativo opcional. Si es NULL, se detecta automáticamente.}

\item{.name}{Nombre de la variable geográfica en data. Si es NULL, se detecta automáticamente.}

\item{.key}{Nombre de la variable clave en los datos geográficos. Si es NULL, se detecta automáticamente.}

\item{file}{Ruta HTML opcional donde guardar el mapa.}

\item{title, subtitle, caption}{Titulo, subtitulo y fuente del mapa.}

\item{labels}{FALSE, TRUE, "name", "value" o "both".}

\item{background}{"none" (sin conexion) u "osm" (calles, requiere internet).}

\item{context}{Incluir limites provinciales y municipales para explorar.}
}
\value{
Documento HTML de clase \code{geodom_html}, invisiblemente si se usa \code{file}.
}
\description{
Incluye el visor, los estilos y las geometrias en un documento HTML. Permite
buscar, seleccionar y consultar territorios, acercar y desplazar el mapa y
explorar provincias y municipios. Las capas de contexto no heredan mediciones.
El fondo predeterminado funciona sin conexion despues de generar el archivo.
}
\examples{
\dontrun{
datos <- data.frame(PROV_CODE = c("01", "25"), valor = c(12, 34))
gd_map_interactive(datos, fill = "valor", .level = "provinces",
                   .name = "PROV_CODE", .key = "PROV_CODE", file = "mapa.html")
}
}
